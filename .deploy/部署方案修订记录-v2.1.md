# 《菲律宾部署方案.xlsx》v2.0 → v2.1 修订记录

| 项 | 内容 |
| --- | --- |
| 修订对象 | `菲律宾部署方案.xlsx`（v2.0，2026-09-22） |
| 修订产出 | `菲律宾部署方案-v2.1-修订版.xlsx`（新文件，原文件保留未改动） |
| 修订依据 | `部署方案评审-2026-09-23.md`（P0 ×5 / P1 ×18 / P2 ×26） |
| 修订日期 | 2026-09-23 |
| 校验方式 | 结构断言 + 依赖时序 + 人力负荷 + XML 完整性（Python）；公式计算由 WPS 表格引擎实测复核 |

---

## 一、结论：评审提出的 49 项问题全部闭环

| 分类 | 数量 | 状态 |
| --- | --- | --- |
| P0 架构级问题 | 5 | 全部闭环（含 2 项口径决策） |
| P1 实施漏项 | 18 | 全部补齐为资源条目 / 落地任务 / 门禁项 |
| P2 排期与数据错误 | 26 | 全部修正 |
| 跨文档同步（`impl_deploy.md`） | 4 处 | 已同步修改 |

**工期口径由 5 天修正为 9 天**——这是本次修订唯一"对外可见"的实质变化，依据见第七节验算。

---

## 二、P0 架构级问题（5 项）

| # | 问题 | 修订动作 | 落点 |
| --- | --- | --- | --- |
| P0-1 | 备 region 容量只有主站点 50%，接管能力不满足 1.5× | 新增「PH 备 HPA min 2 / max 24」「备 region 节点池自动伸缩 min 2 / max 12（96 vCPU）」；配额申请由 32 → **≥96 vCPU**；M4 验收改为"扩到 24 副本并压到 1.5× 主站峰值成功率不降" | 资源清单-新加坡 #19/#20、账号表 E10/K10、落地计划 #44、里程碑 M4 |
| P0-2 | HPA max 16 与 64 vCPU 配额矛盾、缺 cluster-autoscaler、缺 PgBouncer | 新增「cluster-autoscaler（主站 min 4/max 8 = 64 vCPU）」、「PgBouncer / RDS 代理 3 副本」、「连接数预算与参数组」三项资源与三项任务（#41/#42/#43），并入 G11 门禁 | 资源清单-马尼拉 #30/#31/#35、落地计划 #41–#43、门禁 G11 |
| P0-3 | GA 与上游 IP 白名单自相矛盾，且 GA 全表缺失 | **口径决策：本次不启用 GA**，出口由各 region NAT EIP 池直出，白名单 = 马尼拉 4 + 新加坡 4 共 8 个 EIP；GA 列为二期可选优化并写明"启用 GA 后白名单须改为 GA 回源 IP"；新增任务 #56「上游供应商白名单提交与生效确认」 | 说明与总览 B11、资源清单-马尼拉 H8、网络与安全规划 E18/F18、落地计划 #56 |
| P0-4 | 「整站故障 RTO ≤5 min」在单 region 主库下不成立，且 SLA 排除项未带入表格 | sheet7 新增「SLA 指标口径与排除项」9 行区块：服务窗口 / 不可用 / 计划内维护 / 部分降级 / 排除项 / 预算 21.6 分钟 / 达成最低配置 / **故障域边界**（= 接入层 + 计算层，不含 RDS 所在 region 失效）/ 指标阈值口径（验收线 vs 阻断线）；并新增 G12 门禁要求书面确认 | 里程碑与验收 "SLA 指标口径与排除项"、门禁 G12 |
| P0-5 | 交付范围与服务对象不一致（声明服务泰菲，实际只交付菲律宾） | 说明与总览新增「交付范围（本次）」：仅菲律宾（马尼拉 + 新加坡 PH 备），泰国二期；并明确泰国用户接入马尼拉 RTT +55–80 ms 的偏差须书面确认 | 说明与总览 A16、里程碑 "关键风险" 末条 |

---

## 三、P1 实施漏项（18 项 → 全部补齐）

| # | 漏项 | 补齐方式 |
| --- | --- | --- |
| 1 | 代码侧 4 项改造零人时 | 门禁 **G8 由「排期确认」升级为「完成并合并」**；账号表新增说明"研发独立排期，不计入部署人时" |
| 2 | staging / perf 环境缺失 | 新增资源 #33 + 任务 #45（马尼拉命名空间 + 主站 RDS 逻辑隔离 schema）+ 门禁 G13 |
| 2b | staging 用新加坡 RDS 与「新加坡无 RDS」红线冲突 | 书面澄清：staging/perf 改在马尼拉，红线的例外不成立；同步修改 `impl_deploy.md` 7.9 环境矩阵 |
| 3 | 运维访问面缺失 | 新增资源 #38（kubeconfig + RAM→RBAC + API Server 私网端点 + 堡垒机/VPN + 变更审批）+ 任务 #46 + 网络表 2 条安全组规则 |
| 4 | WAF 可被绕过 | ALB 入向由 `0.0.0.0/0` 收敛为 **WAF / DCDN 回源 IP 段**（马尼拉 + 新加坡各一条）+ 任务 #47 |
| 5 | 建议以 CEN / VPC 对等替代 RDS 公网直连 | 列入风险表（含收益/代价）与裁剪预案；任务 #30/#39 保留阻断线控制 |
| 6 | Tair 无预热策略 | 新增任务 #49（接管前 warmup + 缓存命中率验收） |
| 7 | 无 PITR 恢复演练 | 新增任务 #50（恢复 RPO/RTO 实测回填），并入 M5 验收 |
| 8 | 连接数预算未核算 | 新增资源 #35 + 任务 #41 验收（PG 实际连接数 ≤ max_connections × 0.8） |
| 9 | Grafana 缺失 | 新增资源 #37，任务 #26 一并接入 |
| 10 | SESSION_SECRET 轮换无任务无责任人 | 新增任务 #55（双密钥过渡）+ 网络表 D37 改为「180 天（双密钥过渡）」 |
| 11 | 支付回调未限定源 IP | 新增资源 #34 + 任务 #48 + 网络/账号表/资源表 3 处描述同步 |
| 12 | 上游 RPM/TPM 配额未评估 | 新增任务 #51（台账 + 限流参数对齐）+ 风险表专项 |
| 13 | 带宽规格未给 | 新增资源 #36（NAT ≥200 Mbps / EIP ≥100 Mbps）+ 任务 #52 |
| 14 | 成本无量化 | 新增资源 #39（成本看板与预算告警）+ 任务 #52（成本量化表） |
| 15 | DDoS 防护未评估 | 保留为风险表条目（阿里云基础防护清洗阈值需向客户/供应商确认） |
| 16 | 配置热更新一致性无验证任务 | 新增任务 #53（SYNC_FREQUENCY=30 跨节点收敛验证） |
| 17 | 数据库迁移无版本化与回滚策略 | 新增任务 #54（golang-migrate + expand-contract 回滚验证） |
| 18 | 备 region 与泰国备共用集群风险 | 改为"泰国备二期，不共用本集群"，容量与配额单列（G10） |

---

## 四、P2 排期与数据错误（26 项 → 全部修正）

### 4.1 会直接导致执行失败

| # | 问题 | 修正 |
| --- | --- | --- |
| a | T-3 前置期实际 0 天 | 前置期改为真实 **T-5 = 2026-09-23**，D1 = 2026-09-28；门禁清单分 T-5（G1–G7、G9）与 T-3（G8、G10–G13）两段 |
| b | 证书部署时序不可能 | 任务 #3 改为"证书就绪与部署预置"；新增任务 **#40 证书正式部署到 ALB/WAF/DCDN**，排在 ALB(#19)/WAF(#20)/SG ALB(#25) 之后（实际落 D6）；账号表 #18 启动时点改 D6 上午 |
| c | #15 依赖倒挂 | #15 前置保留 `4,12`，重排后落在 S5（晚于 #12 的 S4） |
| d | D5 同槽依赖 + 上线与演练同槽 | #34 前置由 `31,36,37,39` 改为 **`31,33,36,37,38`**；#34 落 S16、#35 落 S17，不同槽 |
| e | J48 负荷公式失效（假绿灯） | 公式改为 `=IF(COUNTIF(J63:AA64,">4")>0,…)` 指向真实负荷行（原引用 J5:S6 任务行） |
| f | 全工期 100% 满负荷 | 工期扩为 9 天（18 时段 / 144 人时），投入 113 人时（78%），保留 31 人时缓冲 |

### 4.2 里程碑与门禁口径

| # | 问题 | 修正 |
| --- | --- | --- |
| g | C4 硬编码 | 改为 `=说明与总览!$B$21+B4`（偏移 −5），全列公式化 |
| h | M1/M2 同日 | 偏移改为 +1 / +3 / +6 / +7 / +8（M1=D2、M2=D4、M3=D7、M4=D8、M5=D9） |
| i | 里程碑与落地计划整体 +1 天 | 两表统一由 D1 基准推算，M 通过标准内容与任务完成日对齐 |
| j | 门禁计数 11 项 | 更正为 **12 项**（J 列实际标 ★ 行数），并说明 T-5 / T-3 归属 |
| k | 漏 G9 | 门禁清单补全 **G1–G13**，A51/A62 文字同步更正 |
| l | ★ 门禁 12 项 ↔ G 编号无映射 | 门禁清单「门禁作用」列内嵌「对应账号表 #N」映射 |
| m | RTT 阈值 70 / 100 并存 | 明确为"验收线 70 ms / 阻断线 100 ms"两个不同概念；失败率 0.1% 补「5 分钟滑动窗口」 |
| n | 状态列口径未定义 | 保留"未开始"并纳入交接项（Runbook 中定义取值集合与更新责任） |

### 4.3 跨表引用错位与数据不一致

| # | 问题 | 修正 |
| --- | --- | --- |
| o | 账号表 I21/I22/I19 引用错位 | I21 → 落地计划 **#40**；I22 → **#32**；#16 启动时点改 D5 上午 |
| p | 前置依赖列混用两套编号 | #28 的 `20(马尼拉)` 改为 `17`（本表命名空间），全部改为本表序号 + 门禁号 |
| q | 资源清单 I9 缺「(前置)」标注 | 改为 `2,17(前置)` |
| r | 数据盘 300G vs 200G | 统一为 **300G**，并同步修改 `impl_deploy.md` 7.2 |
| s | 新加坡节点数三处口径不同 | 统一为"常态 2 节点 / 自动伸缩 2–12；PH 备常态 2 副本 / HPA 2–24"，同步修改 `impl_deploy.md` 7.2 与 7.10 |
| t | 域名混用 likha.com / example-ph.com | 全表统一 **api.likha.com / ops.likha.com**；证书改 `*.likha.com` |
| u | 清单结构描述与实际不符 | ②③ 项数改为 39 / 20 并列出新增条目 |
| v | 新加坡 vSwitch 4×/20 与网络表 3 条不一致 | 统一为 `2×/24 + 2×/20`，网络表补 `vsw-sg-pub-b`（ALB 跨 2 AZ 必需） |
| w | 数量列口径混乱 | 新增条目按实际口径填写；资源表新增行数值/文本分离 |
| x | 新加坡 #9 RDS 落地日 D1、申请方式"—" | 落地日与申请方式统一改为 `—` |
| y | 安全组源/目的混写安全组与跨 region 公网地址 | 跨 VPC 规则改为 **CIDR /32**（`10.1.0.0/16`、`马尼拉 RDS 公网 IP/32`），并注明由 RDS 白名单二次收敛 |
| z | 标题自述"最小开放原则"但 3 条规则全放通 | 标题改为"入向禁止 0.0.0.0/0，仅放行 WAF/DCDN 回源段与对端 CIDR"；3 条全放通已收敛，仅保留出向 443 并注明为必要放通 |

---

## 五、跨文档同步（`impl_deploy.md` 4 处）

| 行 | 原 | 现 |
| --- | --- | --- |
| 7.2 计算/ECS 节点池 | 新加坡备 ≥2（PH 备 + TH 备各 1）；数据盘 200 G | 主站/曼谷常态 4 + 自动伸缩 4–8；新加坡常态 2 + 2–12；数据盘 **300 G**；补"必须开启自动伸缩，否则 HPA 上限会 Pending" |
| 7.9 环境矩阵 | staging / perf 在**新加坡**，用 RDS PG | staging / perf 在**马尼拉**（主站命名空间 + 逻辑隔离 schema），并注明不构成对"新加坡不部署 RDS"红线的例外 |
| 7.10 成本结构 | 新加坡备 region 常态 4×（PH 备 + TH 备各 2） | 常态 2×（PH 2 / TH 2），接管期可扩至 12 节点 / 24 副本（≥ 峰值 16 × 1.5） |
| 7.10 网络 | 建议对上游出口走 GA | 本次按 NAT EIP 直出 + IP 白名单交付，**GA 为二期可选优化**，并写明启用后白名单须改为 GA 回源 IP |

---

## 六、工期口径变更验算

| 项 | v2.0 | v2.1 |
| --- | --- | --- |
| 工期 | 5 天（10 时段） | **9 天（18 时段）**，D1 = 2026-09-28 ~ D9 = 2026-10-06 |
| 任务数 | 39 | **56**（新增 PgBouncer、自动伸缩、证书部署、staging/perf、支付回调、连接数预算、带宽规格、Grafana、运维访问面、成本看板、Tair 预热、PITR 演练、上游配额、白名单确认、HPA×2、安全组收敛、热更新验证、迁移版本化、SESSION_SECRET、主站 HPA） |
| 人时 | 80（10 时段 × 8，**100% 满负荷**） | 113（产能 144，**78%**，保留 31 人时缓冲） |
| 单人负荷 | 每时段恰 4 人时 | 人员A 峰值 4.0 / 合计 57；人员B 峰值 4.0 / 合计 56（均 ≤ 4） |
| 前置期 | T-3 = 0 天 | T-5 = 5 个自然日 |

**9 天的推导**：在"2 人全职 + 单人每时段 ≤ 4 人时 + 现有依赖链"约束下，用随机重排调度器（600 次重启 + 拓扑优先 + 跨时段分摊）求解最短工期，16 时段（8 天）**无可行解**，17 时段可行（M5 收官），故取 18 时段（9 天）并保留缓冲。若要回到 5 天，必须新增第 3 人（约 +64 人时）或按裁剪预案降级交付范围，二者不可兼得。

---

## 七、校验记录

- 结构断言：7 个 sheet 齐全；落地计划表头 30 列（S1–S18 甘特 + 验收/状态/投入）、56 任务行、门禁清单 G1–G13；资源清单 39 / 20 条序号连续
- 依赖时序：56 项任务全部满足"起始时段 > 所有前置完成时段"（0 违规）
- 人力负荷：逐时段逐人核算，峰值 4.00（0 超载）
- 公式复核（WPS 表格引擎实测）：`SUM(F4:F59)=113`、`2*9*8=144`、负荷结论="负荷均衡"、`SUMIFS` 抽样=3.0、里程碑 M5 日期=46301（2026-10-06）、甘特与每时段投入列计算正常
- XML 完整性：全部 part 可解析；原文件 `菲律宾部署方案.xlsx` 未做任何修改

---

## 八、遗留待决策项（需人工确认，非表格可自决）

1. **正式域名**：现统一为 `likha.com` 占位，需替换为正式域名（全表已统一，替换时按域名单一并替换）。
2. **备 region 配额 ≥96 vCPU 是否可批**：这是 P0-1 的硬前提；若批不下来，须走裁剪预案 5（冷备）并在合同层面下调 SLA 口径。
3. **是否评估 CEN / VPC 对等替代公网直连**：收益明显（降抖动、消除公网暴露面、免公网流量费），但 T-5 需增开通项与预算。
4. **泰国站点（曼谷）二期**：需在合同/验收口径中书面确认本次不含泰国。
5. **上游供应商白名单是否接受 8 个 EIP**：部分供应商对白名单条目数有限制。
6. **`impl_deploy.md` 1.3 行「月度不可用预算 ≤ 21.9 分钟」与 8.1 行「21.6 分钟」不一致**（正确值 43,200 × 0.05% = 21.6），本次未改动正文，建议一并校正。
