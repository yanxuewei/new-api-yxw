# 2026-09-20

## impl_tech.md 3.2 表格调整

- 文件:`impl_tech.md` 第 316-331 行「3.2 后端逻辑分层（Go 模块内）」表格。
- 变更:「代表文件」列原为顿号（、）分隔的多文件列表，改为 `<br>` 分隔，使 Markdown 渲染后一个文件一行（表格单元格内不支持真实换行，`<br>` 为标准做法）。
- 该表格是全文档唯一含「代表文件」列的表格；文档此前无 `<br>` 用法。
- 入口行 `main.go` 单文件，未改动。
- 追加（同一节）:入口行的「说明」列 `InitResources（）` 加载清单同样改为 `<br>` 分隔，一行一项。表格其余说明列是句子（非文件清单），未改。
